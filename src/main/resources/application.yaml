spring:
  application:
    name: Market

  datasource:
    url: "jdbc:postgresql://localhost:5432/market"
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
app:
  image:
    bucket: 'C:\Users\rusik\Market\avatars'